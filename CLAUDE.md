@AGENTS.md

# قاعدة أساسية: التطبيق أولاً، ثم PWA

**لا يمرّ أي عمل برمجي في هذا المستودع إلا وقد نزل في التطبيق (`react-native-poc/`) أوّلاً، ثم في الويب (PWA).** بهذا الترتيب، وفي نفس الدفعة.

لا استثناء لميزةٍ "صغيرة"، ولا لإصلاحٍ "عاجل"، ولا لشيء يُقال عنه "أُلحقه بعدين".

**لماذا:** المنتج عميلان يقفان أمام نفس المطعم — كاشير التطبيق وكاشير الويب. وميزةٌ تنزل في أحدهما وحده لا تبدو معطّلة، إنما ناقصة: الزرّ ليس موجوداً، فلا شيء يُشتكى منه. فيهاجر المطعم إلى التطبيق ويكتشف أنه فقد ما اعتاده، أو يفتح الويب فيجد ما لم يره في جواله. وقد وقع هذا فعلاً: عرضُ باركود الولاء على شاشة العميل عاش في الويب وحده أسابيع، وحجوزاتٌ وفنادقُ ما زالت كذلك.

**كيف يُتحقَّق قبل الإغلاق** — لا بالذاكرة، بالقياس:

```bash
# ما يناديه كل عميل من الخادم، ثم الفرق بينهما
grep -ohE "(rpc\('[a-z_]+'|/api/pos/[a-z-]+)" public/pos/rakeen-pos.js \
  | sed "s/rpc('//;s/'$//" | sort -u > /tmp/pwa.txt
while read n; do
  grep -rq -- "$n" react-native-poc/src || echo "ناقص في التطبيق: $n"
done < /tmp/pwa.txt
```

وما يُبنى في الخادم يُبنى **مساراً واحداً يناديه الاثنان**، لا نسخةً لكلٍّ منهما: أي إصلاحٍ فيه يصل العميلين معاً، فلا يفترقان مرّةً أخرى. (انظر `displayBarcodeService.ts` نموذجاً: التطبيق ينادي `/api/pos/show-loyalty-barcode` نفسه الذي يناديه الويب.)

**ما زال متروكاً في الويب وحده** حتى تاريخه — يُنقل إلى التطبيق قبل أي عملٍ جديد فيه:
الفنادق (`create_hotel_booking`، `checkin_hotel_booking`، `finalize_hotel_checkout`، `hotel_room_availability`)،
والحجوزات وقائمة الانتظار (`complete_reservation`، `mark_reservation_ready`، `seat_waitlist_entry`)،
والخدمة المتنقّلة (`start_mobile_service`).
